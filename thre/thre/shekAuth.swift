//
//  shekAuth.swift
//  thre
//
//  Created by الوليد خشيم on 10/02/1445 AH.
//

import SwiftUI

struct shekAuth: View {
    func wt() {  Gwt().Token = "efdgs"}
    var body: some View {
     VStack{   Text( " huijk")
         Gwt().Token == "wrd" ? Gwt().Token = "efdgs" : Gwt().Token = "efdgs"
            Text( " huijk")
       }
    }
}

struct shekAuth_Previews: PreviewProvider {
    static var previews: some View {
        shekAuth()
    }
}
